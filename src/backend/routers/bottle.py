# ============================================================
# bottle.py — Catch Bottle API Router
# Maintainer: Young-cheol (Backend Engineer)
# ============================================================
# 위스키/와인 등 프리미엄 주류 관련 API 엔드포인트를 정의합니다.
# 키워드: Luxury, Exclusive, Night
# ============================================================

from fastapi import APIRouter

router = APIRouter(
    prefix="/bottle",
    tags=["🥃 Catch Bottle"],
    responses={404: {"description": "Not found"}},
)


# ── 헬스 체크 ─────────────────────────────────────────────
@router.get("/")
async def bottle_root():
    """Catch Bottle 엔진 상태 확인."""
    return {
        "service": "Catch Bottle",
        "status": "online",
        "description": "프리미엄 위스키 & 와인 헌팅 엔진",
    }


# ── 추후 확장용 엔드포인트 예시 ───────────────────────────
@router.get("/products")
async def list_products():
    """등록된 프리미엄 주류 목록 조회 (TODO: DB 연동)."""
    return {
        "message": "아직 연동된 데이터가 없습니다. 곧 채워질 예정입니다!",
        "products": [],
    }
